% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logbook_activity_sample_control.R
\name{logbook_activity_sample_control}
\alias{logbook_activity_sample_control}
\title{Gives inconsistencies between the sample and the activity in terms of presence}
\usage{
logbook_activity_sample_control(dataframe1, dataframe2, output)
}
\arguments{
\item{dataframe1}{{\link[base]{data.frame}} expected. Csv or output of the function {\link[furdeb]{data_extraction}}, which must be done before using the logbook_activity_sample_control () function.}

\item{dataframe2}{{\link[base]{data.frame}} expected. Csv or output of the function {\link[furdeb]{data_extraction}}, which must be done before using the logbook_activity_sample_control () function.}

\item{output}{{\link[base]{character}} expected. Kind of expected output. You can choose between "message", "report" or "logical".}
}
\value{
The function returns a {\link[base]{character}} with output is "message", a {\link[base]{data.frame}} with output is "report", a {\link[base]{logical}} with output is "logical"
}
\description{
The purpose of the logbook_activity_sample_control function is to provide a table of data that contains an inconsistency between the sample and the existence of the activity
}
\details{
The input dataframe must contain all these columns for the function to work :
\itemize{
Dataframe 1:
\item{\code{  sample_id}}
}
\itemize{
Dataframe 2:
\item{\code{  sample_id}}
\item{\code{  activity_id}}
}
}
\examples{
#Sample 1 is ok,
#Sample 2 is not linked to an activity
dataframe1 <- data.frame(sample_id = c("1", "2"))
dataframe2 <- data.frame(sample_id = c("1"),
                         activity_id = c("1"))
logbook_activity_sample_control(dataframe1, dataframe2, output = "report")
}
